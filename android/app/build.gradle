apply plugin: 'com.android.application'
// Add the Google Services plugin here at the top
apply plugin: 'com.google.gms.google-services'

android {
    // Standardized 3-segment ID for Play Store
    namespace "com.boxstat.app"
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId "com.boxstat.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        aaptOptions {
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')

    // --- FIREBASE IMPLEMENTATION ---
    // Import the Firebase BoM (Bill of Materials)
    implementation platform('com.google.firebase:firebase-bom:34.11.0')

    // Add Analytics and Cloud Messaging (FCM)
    implementation 'com.google.firebase:firebase-analytics'
    implementation 'com.google.firebase:firebase-messaging'
    // -------------------------------

    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}

apply from: 'capacitor.build.gradle'

// You can keep or remove the 'try' block below,
// but since you have the plugin applied at the top, this is now redundant.
try {
    def servicesJSON = file('google-services.json')
    if (servicesJSON.exists()) {
        // Plugin is already applied at the top of this file
    }
} catch(Exception e) {
    logger.info("google-services.json check skipped")
}